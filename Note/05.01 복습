가장 중요한 필수기능은 
컴포넌트 > render > return > jsx
jsx는 리액트로 작성하는 html이다.

// Update : componentWillReceiveProps() -> shouldComponentUpdate() -> componentWillUpdate() -> render() -> conponentDidUpdate()
componentWillReceiveProps() - 새로운 props를 전달받음
 - old props를 보고 new props와 값이 다르면 update를 true로 인식
shouldComponentUpdate() - 
 - 확인절차

componentWillUpdate() - 컴포넌트가 업데이트 할거라는 단계로 넘어간다.
 - 보통 이단게에서 spinner가 동작한다. 끝나면 spinner를 숨겨준다.
render 

conponentDidUpdate() - 

컴포넌트는 많은 function들을 가진다.

  //willMount에서  api에 요청한다.

  state를 통해서 data를 설정해주고, state값을 변경할 때는 setState를 사용한다.
  state 변경시 render는 재동작한다.

  _ 를 사용하는 이유는 react에는 많은 기능이 있어서 겹치지 않게 하기위함이다.